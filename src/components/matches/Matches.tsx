import React from 'react'

import './Matches.css'

export const Matches = () => {

  // #region Hooks

  // #region Callbacks
  // #endregion

  // #region Rendering
  const classes = ['ap-matches']

  return (
    <div className={classes.join(' ')}>
      MATCHES
    </div>
  )
  // #endregion
}
