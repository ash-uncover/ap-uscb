import React from 'react'

import './Match.css'

export const Match = () => {

  // #region Hooks

  // #region Callbacks
  // #endregion

  // #region Rendering
  const classes = ['ap-match']

  return (
    <div className={classes.join(' ')}>
      MATCH
    </div>
  )
  // #endregion
}
