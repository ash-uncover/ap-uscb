import React from 'react'

import './Player.css'

export const Player = () => {

  // #region Hooks

  // #region Callbacks
  // #endregion

  // #region Rendering
  const classes = ['ap-player']

  return (
    <div className={classes.join(' ')}>
      PLAYER
    </div>
  )
  // #endregion
}
