import React from 'react'

import './Players.css'

export const Players = () => {

  // #region Hooks

  // #region Callbacks
  // #endregion

  // #region Rendering
  const classes = ['ap-players']

  return (
    <div className={classes.join(' ')}>
      PLAYERS
    </div>
  )
  // #endregion
}
